{{ if $.Values.isStandalone }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.project.name }}-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: {{ .Values.repoUrl }}
{{end}}