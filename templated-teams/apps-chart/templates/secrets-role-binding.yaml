apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: secret-reader-binding-{{ .Values.projectName }}-{{ .Values.kargo.stage }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: secret-reader
subjects:
- kind: ServiceAccount
  name: default
  namespace: '{{ .Values.projectName }}-{{ .Values.kargo.stage }}'