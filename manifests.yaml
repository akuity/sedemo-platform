apiVersion: apps/v1
kind: Deployment
metadata:
  name: team-emily-app
spec:
  progressDeadlineSeconds: 9
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: team-emily-app
  template:
    metadata:
      labels:
        app: team-emily-app
    spec:
      containers:
      - image: ghcr.io/emilyxinyi/guestbook:0.1.33
        name: guestbook
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: team-emily-app
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: team-emily-app
