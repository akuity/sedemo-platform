apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: demo-downstream
  namespace: argocd
spec:
  description: Multi-tenant trading platform — layered config + release train demo
  sourceRepos:
    - '*'
  destinations:
    - server: '*'
      name: '*'
      namespace: '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
