config:
  customerId: crd-0002
  customerTier: premium
  maxConnections: 500

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

featureFlags:
  advancedAnalytics: true
