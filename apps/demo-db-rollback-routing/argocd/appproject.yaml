apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: demo-db-rollback-routing
  namespace: argocd
spec:
  description: DB migrations, auto-rollback, and freight routing demo
  sourceRepos:
    - '*'
  destinations:
    - server: '*'
      name: '*'
      namespace: '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
