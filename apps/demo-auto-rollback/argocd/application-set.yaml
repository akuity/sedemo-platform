apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: demo-auto-rollback
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - stage: dev
      - stage: staging 
      - stage: prod-amer-east
      - stage: prod-amer-west
  template:
    metadata:
      name: demo-auto-rollback-{{stage}}
      annotations:
        kargo.akuity.io/authorized-stage: demo-auto-rollback:{{stage}}
        notifications.argoproj.io/subscriptions: |
          - trigger: [on-sync-succeeded, on-sync-running]
            destinations:
              - service: sedemo
                recipients: [argo-notifications]
    spec:
      project: demo-auto-rollback
      source:
        repoURL: https://github.com/akuity/sedemo-monorepo
        targetRevision: env-rollback/{{stage}}
        path: .
      destination:
        name: sedemo-primary
        namespace: demo-rollbacks-{{stage}}
      syncPolicy:
        automated:
          prune: true
          enabled: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true